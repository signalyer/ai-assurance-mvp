# Production build env for team-portal SPA.
# Vite reads this automatically during `npm run build` (mode=production).
# Value is the public engine API base URL — no secrets in this file.
VITE_API_BASE_URL=https://aigovern.sandboxhub.co/api/v1
