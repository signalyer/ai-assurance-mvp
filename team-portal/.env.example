# Team Workspace SPA — environment variables
# Copy to .env.local for dev. Never commit .env.local (see .gitignore).

# Engine API base URL. Dev uses the vite proxy (/api/v1 forwarded to localhost:8000).
# Prod (staging URL): https://api.aigovern.sandboxhub.co/api/v1
VITE_API_BASE_URL=/api/v1
