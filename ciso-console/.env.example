# CISO Console — local dev environment
# Copy to .env.local (never commit .env.local)

# Engine API base URL. Dev proxy handles this automatically — set only for
# standalone / production deployments pointing at api.aigovern.sandboxhub.co.
# VITE_API_BASE_URL=https://api.aigovern.sandboxhub.co/api/v1
