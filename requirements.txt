# Core LLM APIs
anthropic>=0.40.0
openai>=1.0.0

# Tracing & Observability
langfuse>=4.0.0

# Evaluation (replacing DeepEval with Ragas)
ragas>=0.1.0
deepeval>=0.21.0

# Adversarial Testing
garak>=0.5.0

# Web Framework
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Database (for future multi-tenant)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
psycopg[binary]>=3.2
psycopg-pool>=3.2
alembic>=1.13.0

# Security & Encryption
cryptography>=41.0.0
bcrypt>=4.1.0
pyjwt>=2.8.0
python-jose>=3.3.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Configuration & Environment
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# API & Integration
requests>=2.30.0
httpx>=0.27.0
azure-search-documents>=11.4.0
grpcio>=1.60.0
grpcio-tools>=1.60.0

# CLI
typer>=0.12

# Utilities
python-dateutil>=2.8.0
pytz>=2023.3
PyYAML>=6.0.0

# Starlette SessionMiddleware signer — required at module-load time by
# middleware/auth.py. Previously only in requirements-deploy.txt, causing
# CI to fail on OpenAPI export (Session 17 / 2026-05-24). Same drift class
# as PyYAML (S12) and pydantic-settings (S12B): runtime deps must live here,
# not only in requirements-deploy.txt.
itsdangerous>=2.1.2

# Observability — Session 10
opentelemetry-sdk>=1.27
azure-monitor-opentelemetry-exporter>=1.0.0b30
prometheus-client>=0.20

# File locking — audit_chain cross-process advisory lock (Session 10)
portalocker>=2.10

# LRU cache for RTF completed_cache (Session 10)
cachetools>=5.3
