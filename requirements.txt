# Core LLM APIs
anthropic>=0.40.0
openai>=1.0.0

# Tracing & Observability
langfuse>=4.0.0

# Evaluation (replacing DeepEval with Ragas)
ragas>=0.1.0
deepeval>=0.21.0

# Adversarial Testing
garak>=0.5.0

# Web Framework
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Database (for future multi-tenant)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
psycopg[binary]>=3.2
psycopg-pool>=3.2
alembic>=1.13.0

# Security & Encryption
cryptography>=41.0.0
bcrypt>=4.1.0
pyjwt>=2.8.0
python-jose>=3.3.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Configuration & Environment
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# API & Integration
requests>=2.30.0
httpx>=0.27.0
azure-search-documents>=11.4.0
grpcio>=1.60.0
grpcio-tools>=1.60.0

# CLI
typer>=0.12

# Utilities
python-dateutil>=2.8.0
pytz>=2023.3
PyYAML>=6.0.0

# Observability — Session 10
# NOTE: azure-monitor-opentelemetry (the DISTRO) bundles the exporter + the
# auto-instrumentors and exposes configure_azure_monitor() which wires
# traces + metrics + logs in one call. The bare exporter package, used in
# isolation, ships zero instrumentation — spans are never created. This was
# the silent failure caught in Day-12 verification (workspace had no data
# despite the connection string being set and init code running).
opentelemetry-sdk>=1.27
azure-monitor-opentelemetry>=1.6
opentelemetry-instrumentation-fastapi>=0.50b0
prometheus-client>=0.20

# File locking — audit_chain cross-process advisory lock (Session 10)
portalocker>=2.10

# LRU cache for RTF completed_cache (Session 10)
cachetools>=5.3
