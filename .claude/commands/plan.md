Read the most recent docs/plans/SESSION-NN-*.md file (or the one I just named).

Before writing any code, output:
1. The decorator chain order from ARCHITECTURE.md
2. Every file you will CREATE in this session, with one-line purpose
3. Every file you will MODIFY in this session, with the exact function/line that changes
4. The two most critical architectural constraints relevant to this session
5. An explicit "Will NOT build in this session" list
6. The acceptance criteria with runnable assertions

Then wait for my approval. Do not write any code until I say "go" or "approved".
