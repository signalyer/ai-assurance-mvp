[pytest]
# Use a project-local tmp directory to sidestep Windows %TEMP% ACL quirks that
# can lock %TEMP%/pytest-of-<user> across runs. The directory is gitignored.
addopts = --basetemp=./_pytest_tmp
filterwarnings =
    ignore::DeprecationWarning:pdf_report
asyncio_mode = strict
